# Staging Smoke Run — 2026-09-16

- **Procedure:** `STAGING_AND_RELEASE_PLAN.md`, Smoke Procedure.
- **Authorization:** owner-approved, read-only, scoped to project `IAQP Staging`, including a match-only check of configuration values and migration parity with a locally stored password.
- **Recording rule:** statuses, booleans, and version numbers only. Project references, domains, hostnames, and values stay outside Git.

| # | Check | Result |
|---|---|---|
| 1 | Deployment | `SUCCESS` from branch `staging`; deployed revision equals the `staging` tip |
| 2 | Environment | `APP_ENV` is `staging` |
| 3 | Origins | No wildcard in `ALLOWED_ORIGINS` |
| 4 | Database target | `DATABASE_URL` host is Supabase and carries the `IAQP Staging` project reference |
| 5 | Wallet target | `QP_URL` is `https`, its host is in `QP_ALLOWED_HOSTS`, it equals the QuartzPlay staging API host, and no production host is allowlisted |
| 6 | Liveness | `GET /salud` 200, `ok: true`, 2 live tables |
| 7 | Readiness | `GET /ready` 200 `{"ok": true}` |
| 8 | CORS | Allowed origin receives `Access-Control-Allow-Origin`; disallowed origin does not |
| 9 | Migration parity | Local `20260910150000` equals remote `20260910150000`; parity confirmed with the owner's locally stored password (never printed) |

**Result: 9 of 9 checks passed.**

## Notes

- `ANTHROPIC_API_KEY` is absent in staging; the dealer chat returns an unconfigured result. Out of smoke scope; recorded as a known gap.
- No game round, bet, or wallet mutation was executed.
